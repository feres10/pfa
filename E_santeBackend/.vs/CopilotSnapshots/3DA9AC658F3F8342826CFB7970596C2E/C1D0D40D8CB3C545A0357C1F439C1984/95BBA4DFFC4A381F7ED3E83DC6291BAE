@page "/register"
@using E_santeFrontend.Models
@inject E_santeFrontend.Services.AuthService Auth
@inject NavigationManager Nav

<PageTitle>Inscription</PageTitle>

<div class="container py-4">
  <a class="text-muted d-inline-flex align-items-center mb-3" href="/home">? Retour � l'accueil</a>

  <div class="d-flex flex-column align-items-center">
    <div class="rounded-circle bg-dark text-white d-flex align-items-center justify-content-center" style="width:56px;height:56px;">
      <span class="fw-bold">?</span>
    </div>
    <h2 class="mt-3 mb-1">E?Sant�</h2>
    <p class="text-muted mb-4">Cr�ez votre compte</p>

    <div class="card shadow-sm" style="max-width:520px;width:100%;">
      <div class="card-body">
        <EditForm Model="model" OnValidSubmit="HandleRegister">
          <div class="row g-3">
            <div class="col-12 col-md-6">
              <label class="form-label">Nom</label>
              <InputText class="form-control" @bind-Value="model.Nom" />
            </div>
            <div class="col-12 col-md-6">
              <label class="form-label">Pr�nom</label>
              <InputText class="form-control" @bind-Value="model.Prenom" />
            </div>
            <div class="col-12">
              <label class="form-label">Email</label>
              <InputText class="form-control" @bind-Value="model.Email" />
            </div>
            <div class="col-12">
              <label class="form-label">T�l�phone</label>
              <InputText class="form-control" @bind-Value="model.Telephone" />
            </div>
            <div class="col-12">
              <label class="form-label">Mot de passe</label>
              <InputText class="form-control" @bind-Value="model.Password" type="password" />
            </div>
          </div>
          <button class="btn btn-primary w-100 mt-3" type="submit">Cr�er un compte</button>
        </EditForm>
        <div class="text-center mt-3">
          <span class="text-muted">D�j� inscrit ?</span>
          <a href="/login" class="ms-1">Se connecter</a>
        </div>
      </div>
    </div>
  </div>
</div>

@code {
  private RegisterDto model = new();

  private async Task HandleRegister()
  {
    // Ensure role is Patient for normal signup
    model.Role = "Patient";
    var ok = await Auth.RegisterAsync(model);
    if (ok)
    {
      Nav.NavigateTo("/login");
    }
  }
}
